Funcionamento:
Cria um dicionário vazio produtos.

Usa um loop for para cadastrar 5 produtos.

Para cada produto:

Pede o nome (string).

Valida o preço (garante que seja um número positivo).

Armazena cada produto no dicionário (produtos[nome] = preco).

Calcula o total usando sum(produtos.values()).

Exibe a lista de produtos e o valor total formatado.
